🔹 Подготовка к чтению файла
1️⃣ Открыть файл и получить ссылку на него (если файл недоступен, завершить работу).
2️⃣ Выделить память для временного буфера, в который будет загружаться новая порция данных.
3️⃣ Считать данные из файла (размером, указанным при компиляции), добавив их к уже накопленным.

🔹 Обработка считанных данных
4️⃣ Проверить, есть ли конец строки в накопленных данных:

Если конец строки найден → сформировать отдельную часть до этого символа.
Если нет → продолжить считывание, пока не накопится полная строка.
5️⃣ Создать копию данных, которые будут возвращены, чтобы передать их вызывающей функции.
6️⃣ Обновить накопленные данные, удалив уже обработанную часть.

🔹 Очистка памяти и завершение
7️⃣ Если больше нечего сохранять, полностью очистить накопленные данные, чтобы избежать утечек памяти.
8️⃣ Освободить временный буфер перед выходом, чтобы не оставлять лишние выделенные ресурсы.
9️⃣ Закрыть файл после завершения работы, если чтение больше не требуется.